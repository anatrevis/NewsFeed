# ===========================================
# NewsFeed Application Environment Variables
# ===========================================
# Only NEWS_API_KEY is required - get it free at https://newsapi.org/register

# NEWS API (REQUIRED)
# Get your free API key at https://newsapi.org/register
NEWS_API_KEY=your_news_api_key_here

# ===========================================
# Below values are pre-configured - no changes needed for local development
# ===========================================

# PostgreSQL
POSTGRES_USER=newsfeed
POSTGRES_PASSWORD=newsfeed_secret
POSTGRES_DB=newsfeed

# Authentik
# Generate a random secret key (at least 50 characters recommended)
# You can generate one with: openssl rand -hex 32
# Or use: python -c "import secrets; print(secrets.token_hex(32))"
AUTHENTIK_SECRET_KEY=change-me-generate-a-random-secret-key
AUTHENTIK_CLIENT_ID=newsfeed-app

# CORS
CORS_ORIGINS=http://localhost:3000

# Logging
ENVIRONMENT=development
LOG_LEVEL=INFO

# OpenAI (OPTIONAL - for AI Summary feature)
# Get your API key at https://platform.openai.com/api-keys
# Leave empty to disable AI Summary feature
OPENAI_API_KEY=
